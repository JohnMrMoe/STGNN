/*
// X
using namespace std;

class Dataset {
private:
  void alloc_data();

public:
  // ??? data;
  float mean;
  float stnd;

  // ??? <type data> get_data();
  vector get_stats();
  vector get_len();
  // ??? <type data> z_inverse();

}

//??? seq_gen(len_seq, data_seq, offset, n_frame, n_route, day_slot, C_0=1):
//data data_gen(char * file_path, ??? data_config, size_t n_route, size_t n_frame=21, size_t day_slot=288) {
//??? gen_batch(inputs, batch_size, dynamic_batch=False, shuffle=False):
}
*/
